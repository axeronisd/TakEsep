name: takesep_design_system
description: Shared design system (UI Kit) for all TakEsep ecosystem apps. Contains theme, colors, typography, and reusable widgets.

version: 0.1.0

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0

flutter:
  uses-material-design: true
