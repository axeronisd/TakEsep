name: takesep_core
description: Shared core package for TakEsep ecosystem — models, DTOs, constants, and business logic utilities.

version: 0.1.0

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0
  equatable: ^2.0.5

dev_dependencies:
  build_runner: ^2.4.8
  freezed: ^2.5.2
  json_serializable: ^6.7.1
